name: build

on:
  push:
  pull_request:

permissions:
  contents: read

jobs:
  windows:
    runs-on: windows-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-dotnet@v4
        with:
          dotnet-version: 10.0.x
      - name: Build
        run: dotnet build SonarMiniMixer.slnx -c Release --nologo
      - name: Test
        run: dotnet run --project SonarMiniMixer.Tests/SonarMiniMixer.Tests.csproj -c Release
      - name: Publish app
        run: dotnet publish SonarMiniMixer.App/SonarMiniMixer.App.csproj -c Release -r win-x64 --self-contained true -p:PublishSingleFile=true -p:IncludeNativeLibrariesForSelfExtract=true -o artifacts/publish
      - name: Publish CLI
        run: dotnet publish SonarMiniMixer.Cli/SonarMiniMixer.Cli.csproj -c Release -r win-x64 --self-contained true -p:PublishSingleFile=true -o artifacts/publish
